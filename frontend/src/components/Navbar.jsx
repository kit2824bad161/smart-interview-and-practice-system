import { Link } from 'react-router-dom';
import { ArrowRight, BrainCircuit, Menu, X } from 'lucide-react';
import { useState } from 'react';
import Button from './Button';
export default function Navbar({ publicNav = false }) { const [open, setOpen] = useState(false); const links = publicNav ? [['#features','Features'],['#how-it-works','How it works'],['#about','About']] : [['/dashboard','Overview'],['/interview/setup','New interview'],['/interview/history','History']]; return <header className="relative z-30 border-b border-slate-200/70 bg-white/80 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8"><Link to={publicNav ? '/' : '/dashboard'} className="flex items-center gap-2.5"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-ink text-white shadow-lg"><BrainCircuit size={20} /></span><span className="font-display text-lg font-bold tracking-tight">SmartInterview <span className="text-accent">AI</span></span></Link><nav className="hidden items-center gap-8 md:flex">{links.map(([to,label]) => <Link className="text-sm font-semibold text-slate-500 transition hover:text-ink" key={to} to={to}>{label}</Link>)}</nav><div className="hidden items-center gap-3 md:flex"><Link to="/login" className="px-3 text-sm font-bold text-slate-600 hover:text-accent">Login</Link><Link to="/register"><Button className="px-4 py-2.5">Get started <ArrowRight size={15} /></Button></Link></div><button className="rounded-lg p-2 text-slate-600 md:hidden" onClick={() => setOpen(!open)} aria-label="Toggle menu">{open ? <X /> : <Menu />}</button></div>{open && <div className="border-t border-slate-100 bg-white px-5 py-4 md:hidden">{links.map(([to,label]) => <Link className="block py-3 text-sm font-semibold" key={to} to={to} onClick={() => setOpen(false)}>{label}</Link>)}<div className="mt-3 flex gap-3"><Link to="/login" className="btn-secondary flex-1">Login</Link><Link to="/register" className="btn-primary flex-1">Get started</Link></div></div>}</header>; }
