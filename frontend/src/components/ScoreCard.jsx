import { ArrowUpRight } from 'lucide-react';
import Card from './Card';
export default function ScoreCard({ label, value, icon: Icon, tone = 'indigo', trend }) { const colors = { indigo: 'bg-indigo-50 text-indigo-600', mint: 'bg-emerald-50 text-emerald-600', amber: 'bg-amber-50 text-amber-600', violet: 'bg-violet-50 text-violet-600' }; return <Card className="p-5"><div className="flex items-start justify-between"><div className={`flex h-10 w-10 items-center justify-center rounded-xl ${colors[tone]}`}><Icon size={19} /></div>{trend && <span className="flex items-center gap-1 text-xs font-bold text-emerald-600"><ArrowUpRight size={14} />{trend}</span>}</div><p className="mt-5 text-2xl font-bold tracking-tight">{value}</p><p className="mt-1 text-xs font-semibold text-slate-500">{label}</p></Card>; }
